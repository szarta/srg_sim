Design Notes
============

Longer-term design notes and open questions that outlive a single change.

The **pinned, authoritative architecture** — scope, domain model, Effect IR,
rules-text pipeline, game state, turn loop, policy interface, game-log schema,
module layout, milestones, and validation plan — lives in :file:`DESIGN.md` at
the repository root. It is the review gate and the single source of truth; this
section supplements it, it does not replace it.

.. toctree::
   :maxdepth: 1

   overview
   open_questions
