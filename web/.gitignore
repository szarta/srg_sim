node_modules/
dist/
# NOTE: src/pkg/ (wasm-bindgen output) IS committed so the frontend can vendor a
# known-good artifact without a local Rust toolchain (FRONTEND_INTEGRATION_BRIEF.md).
# Refresh it from the current engine with `invoke release-web` and commit the result.
